| system | oss_license | primary_environment | sql_api | formula_interface | family_link | factor_handling | interaction_handling | offset | weight | model_representation | relation_in | relation_out | named_arguments | null_handling | metadata_representation | prediction_api | reproducibility_priority | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FbSQL | MIT | PostgreSQL extension | yes (fit_glm / predict_glm as set-returning functions) | yes (R formula string with R glm() semantics) | gaussian/identity, binomial/logit (PoC) | automatic (glm conventions: sorted levels; treatment contrasts) | not yet (explicit error at predict) | no (deliberately out of first-paper scope) | no (deliberately out of first-paper scope) | single relation (coefficient rows + repeated model-level columns) | yes (SQL string argument) | yes (one relation) | yes (PostgreSQL => syntax) | Complete Case Analysis reported via n_obs/n_used/n_dropped; NULL predictors predict NULL | jsonb metadata column (xlevels/contrasts/terms; SQL-inspectable) | relation in -> relation out with <response>_predicted column | R parity locked by pg_regress tests | Proposed system (this paper); every cell verified by the pg_regress suite in the FbSQL repo |
| Apache MADlib | Apache-2.0 | PostgreSQL / Greenplum extension | yes (procedure-style training functions + scalar predict functions) | no (independent variables as an SQL array expression string) | glm(): gaussian, binomial, poisson, gamma, inverse gaussian with link options; logregr_train(): binomial/logit | manual one-hot by the user (encode_categorical_variables exists as a separate preprocessing step) | manual (hand-built columns / array expressions) | no (not among glm()/logregr_train() parameters) | no (not among glm()/logregr_train() parameters) | two side-effect tables: model table with parallel arrays (coef/std_err/z_stats/p_values; no term names) + <out>_summary | partial (source table/view name as string; no inline subquery) | no (fit returns void and writes tables by name; predict is a scalar function per row) | positional in documented usage (=> possible in principle) | complete case at fit with num_rows_processed / num_missing_rows_skipped counters; NULL feature predicts NULL | <out>_summary table stores call parameter strings (no structured factor levels) | scalar predict functions over the coef array + a hand-built feature array (manual join with the model table) | deterministic IRLS (tolerance/max_iter only; no RNG); no stated reproducibility framework | Tier 1 (required experiment); verified with 1.21.0 on PostgreSQL 11 (docker/madlib); unseen factor levels silently scored as reference level |
| Apache Spark MLlib | Apache-2.0 | Spark (DataFrame / Pipeline API; Spark SQL for relation definition) | no as SQL functions (spark.sql defines relations; ML is DataFrame/Pipeline API) | formula-shaped via RFormula but NOT R semantics (frequency-ordered levels; least-frequent level as reference -- observed) | GeneralizedLinearRegression: gaussian, binomial, poisson, gamma, tweedie with link options | automatic one-hot via RFormula but with frequency-based reference level and gender_F-style names in ML attribute metadata (observed) | yes (RFormula documents ':' and '*'; not exercised) | yes (offsetCol on GeneralizedLinearRegression; not exercised) | yes (weightCol on GeneralizedLinearRegression; not exercised) | PipelineModel (JVM object graph; persisted as a directory; never a relation) | yes at the DataFrame level (not SQL) | transform() returns the input DataFrame + appended prediction columns (DataFrame-level closure; not SQL-level) | host-language keyword arguments (not SQL) | default handleInvalid='error' fails on NULL at transform (observed); 'skip' silently drops rows changing output granularity (observed) | ML attribute metadata on the features column + fitted stage state inside the PipelineModel | model.transform(dataframe) appending 'prediction' (response scale for GLR) | GLR/IRLS deterministic; seed parameters for stochastic algorithms | Tier 2; verified with Spark 3.5.1 local mode (docker/spark); predictions match R/FbSQL to 4 decimals, coefficients only after reparameterization |
| PostgresML | MIT | PostgreSQL extension (Rust/pgrx + bundled Python ML runtime) | yes (pgml.train / pgml.predict / pgml.predict_proba) | no (task + algorithm names; every non-label column of the named relation is a feature) | none (no family/link concept; algorithm-centric: linear / xgboost / ...; no SE / p-values / CI) | text columns label-encoded to ordinal reals by default (observed F=1 M=2 Other=3); other encoders configurable via preprocess (not verified) | TBD | TBD | TBD | serialized binary model in pgml.files + catalog tables (projects/models/snapshots/deployments); never a user-visible relation | partial (table/view name string; no inline subquery) | no (train returns a status row and writes catalog state; predict is a scalar per row) | yes (documented name => value usage) | fit-time per-column impute policy (default: error); predict raises 'array contains NULL' on NULL features (hard error) | per-column type / preprocessor / statistics / category mappings in pgml.snapshots (system catalogs) | scalar predict over a homogeneous REAL array (or ROW form) referencing the project name; deployed-model indirection | deterministic split (test_sampling: last) but no exposed estimator seed; single-class test split aborts training | Tier 2; verified with 2.7.12 on bundled PostgreSQL 15 (official docker image); prediction returns class labels, probabilities via predict_proba |
| Apache Hivemall | Apache-2.0 | Hive / HiveQL UDF-UDAF-UDTF (Apache Incubator; retired 2022-09-01) | yes (UDF/UDAF calls inside HiveQL queries) | no (feature arrays of 'index:value' strings assembled by hand; vectorization helper UDFs) | no GLM family abstraction (per-algorithm training functions such as logress for logistic regression) | manual vectorization by the user (helper UDFs for encoding) | TBD (manual feature construction in principle) | TBD (not documented in reviewed tutorials) | TBD (not documented in reviewed tutorials) | a genuine table (feature; weight) -- but feature is an index without term semantics | yes (HiveQL queries over relations) | partial (prediction is hand-assembled SQL: explode features + join the model table + sigmoid(sum(weight*value)) by hand) | no (positional UDF arguments; hyperparameters packed into one option string like '-total_steps 10') | TBD (not documented in reviewed tutorials) | none (model table carries only feature and weight; no types or level information) | manual SQL assembly (LATERAL VIEW explode + LEFT OUTER JOIN + aggregate + link function by hand) | SGD-based online learning with an autonomic learning rate; seed not documented | Tier 3 (literature-based; not executed; sources in results/summary/related_work_notes.csv); closest to relational closure among the compared systems but with all statistical semantics hand-assembled |
| H2O-3 + Sparkling Water | Apache-2.0 | H2O cluster; Sparkling Water bridges Spark DataFrames to H2OFrames (SQL-adjacent; not a SQL API) | no (R/Python/Flow APIs; Spark SQL reaches it only through DataFrame-to-H2OFrame conversion) | no (x/y column-list specification) | very rich: gaussian, binomial, fractionalbinomial, quasibinomial, multinomial, ordinal, poisson, negativebinomial, gamma, tweedie; links identity/logit/log/inverse/tweedie/ologit | automatic internal encoding (manual one-hot explicitly discouraged); with default regularization ALL levels kept -- a reference level is dropped only when lambda=0 | yes (interactions / interaction_pairs parameters) | yes (offset_column) | yes (weights_column) | binary model object in the H2O cluster (exportable as MOJO/POJO); never a relation | no (H2OFrame; converted from Spark DataFrames via Sparkling Water) | no (model.predict(frame) returns an H2OFrame from the model object) | host-language keyword arguments (not SQL) | missing_values_handling: MeanImputation (default) / Skip / PlugValues -- the default imputes rather than complete-case | inside the model object (not user-visible relations) | model.predict(frame) returning a frame; SE / p-values only on request (compute_p_values with IRLSM) | seed parameter (default -1 = time-based); default fits are regularized (elastic net alpha 0.5) so glm() parity requires lambda=0 | Tier 3 (literature-based; not executed; sources in results/summary/related_work_notes.csv); statistically the richest GLM among compared systems; unseen factor levels at scoring silently replaced by the most frequent training level (default) or ignored (Skip) |
